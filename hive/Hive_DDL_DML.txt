1,John,50000
2,Alice,60000
3,Bob,45000
4,Emma,70000
5,David,52000

Save as:

emp.txt
Hive DDL Commands
CREATE DATABASE company_db;

SHOW DATABASES;

USE company_db;

CREATE TABLE employees (
id INT,
name STRING,
salary FLOAT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ',';

DESCRIBE employees;

ALTER TABLE employees ADD COLUMNS(age INT);

ALTER TABLE employees RENAME TO staff;

DROP TABLE staff;

TRUNCATE TABLE employees;

DROP DATABASE company_db;
Hive DML Commands
USE company_db;

LOAD DATA LOCAL INPATH '/home/sahilgupta/emp.txt'
INTO TABLE employees;

SELECT * FROM employees;

SELECT * FROM employees
WHERE salary > 40000;

CREATE TABLE high_salary AS
SELECT * FROM employees
WHERE salary > 50000;

INSERT INTO employees
SELECT * FROM high_salary;

INSERT OVERWRITE TABLE employees
SELECT * FROM employees
WHERE id != 1;

INSERT OVERWRITE TABLE employees
SELECT
id,
name,
CASE
WHEN id = 2 THEN salary + 5000
ELSE salary
END AS salary
FROM employees;

INSERT OVERWRITE DIRECTORY '/output/employees'
SELECT * FROM employees;